encoder:
  base_channels: 64
  pooling: stats         # stats | attention | gem  -> to benchmark
  embedding_dim: 512     # h (used for rertieval)
projection:
  hidden_dim: 512
  output_dim: 128        # z (only for the loss)