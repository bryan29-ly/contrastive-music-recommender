encoder:
  base_channels: 64      # h dim = base_channels*8 (x2 if pooling=stats)
  pooling: stats         # stats | attention | gem  -> to benchmark
projection:
  hidden_dim: 512
  output_dim: 128        # z (only for the contrastive loss)